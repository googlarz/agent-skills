name: Zip Packages

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  check-zips:
    name: Verify skill zips are up to date
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check zip packages
        run: bash scripts/package-skills.sh --check
